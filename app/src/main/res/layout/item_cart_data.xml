<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:padding="8dp">

    <ImageView
        android:id="@+id/checkBox"
        android:layout_width="16sp"
        android:layout_height="16sp"
        android:layout_gravity="center_vertical"
        android:src="@drawable/round"
        android:visibility="visible"
        app:layout_constraintBottom_toBottomOf="@+id/lessonDetailsTextView"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="@+id/lessonDetailsTextView" />

    <TextView
        android:id="@+id/lessonDetailsTextView"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_margin="8dp"
        android:text="Lesson Details"
        android:textColor="@color/black"
        android:textSize="16sp"
        app:layout_constraintBottom_toTopOf="@+id/amountTextView"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toEndOf="@+id/checkBox"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/amountTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="TextView"
        android:textSize="12sp"
        app:layout_constraintBottom_toTopOf="@+id/termTextView"
        app:layout_constraintStart_toStartOf="@+id/lessonDetailsTextView"
        app:layout_constraintTop_toBottomOf="@+id/lessonDetailsTextView" />

    <TextView
        android:id="@+id/termTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        android:text="TextView"
        android:textSize="12sp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="@+id/amountTextView"
        app:layout_constraintTop_toBottomOf="@+id/amountTextView" />

</androidx.constraintlayout.widget.ConstraintLayout>